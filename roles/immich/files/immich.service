[Unit]
Description=immich via podman compose

[Service]
WorkingDirectory=%h/immich
Type=simple
Restart=always
ExecStart=/usr/bin/podman compose up --remove-orphans
ExecStop=/usr/bin/podman compose down

[Install]
WantedBy=default.target
