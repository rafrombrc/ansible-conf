---
- hosts: biggie
  vars_files:
    - config/base.yml
    - config/biggie.yml
    - config/gui.yml
    - config/dev.yml
    - config/biggie-vault.yml

  handlers:
    - import_tasks: handlers/main.yml

  roles:
    - role: swapfile
      become: true
      tags: swapfile

    - role: set-hostname
      become: true
      tags: set-hostname

    - role: sudoers
      become: true
      tags: sudoers

    - role: rob-user
      become: true
      tags: rob-user

    - role: arch-base
      become: true
      tags: arch-base

    - role: ssh-setup
      tags: ssh-setup

